import { GeneralCategory } from './GeneralCategory';
import { EditorCategory } from './EditorCategory';
import { StorageCategory } from './StorageCategory';

export const CATEGORY_COMPONENTS = {
  'categories.general': GeneralCategory,
  'categories.editor': EditorCategory,
  'categories.storage': StorageCategory,
};
